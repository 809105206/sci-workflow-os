# SCI Workflow OS · Codex 接管协议

## 会话启动

1. 将本仓库视为可持续科研工作区，不要从头熟悉全部文件。
2. 优先使用 CodeGraph MCP 获取代码结构；若工具不可用，再使用 `rg` 和定向读取。
3. 运行 `uv run --frozen sciops codex resume --json`。优先使用 `context_bundle` 中的当前目标、阶段、下一动作、工作集、有效语义记忆和最近事件；仅按 `read_priority` 定向读取。若记忆未初始化，先运行 `sciops memory init <project>`。
4. 用户已给出研究方向且没有活动项目时，直接初始化 `workspace/<short-slug>`，随后执行 `sciops codex activate`。没有项目且用户未给方向时，先询问宽泛研究领域或方向及已有数据/资源约束；不得把上一项目的题目、数据、方法、文献或结论作为默认值。存在多个候选项目且无法判断时才询问。
5. 先继续 `next_actions` 中第一个未完成动作，再扩展任务。不要重复已记录的检索、决策、实验或写作。
6. 用户明确要求新论文、新项目或无关的新方向时，即使已有活动项目也要另建工作区，不得覆盖或改造成旧项目。活动项目通过 G10 后先保持归档，再询问下一研究方向。

## 科研执行

- 学术任务使用仓库 Skill `run-sci-workflow`，并以 `SCI.md` 的 G0–G10 阶段门为质量标准。
- 文献检索先保存题名、摘要、DOI/正规落地页和来源，再由研究者决定全文下载与正式纳入。
- 引用、数值、实验结果和期刊信息必须可核验；禁止编造来源、DOI、样本、指标或结论。
- 学术稿件仅使用陈述句，删除对话式表达、生成过程说明、无证据强化和无关内容。
- 未经原文核验的题录只能进入候选池，不能作为正文论据。
- 每个实质性里程碑后执行 `sciops codex checkpoint`，更新 `research-state.yaml` 的已完成动作、决策、阻塞项和下一步。
- 项目文件是事实源。长期记忆只保留精炼的决策、带来源事实、约束和经验；过期决策使用 `sciops memory remember --supersedes <ID>` 替代，不删除审计轨迹。完整历史只在需要时使用 `sciops memory search` 回查。
- 不把原始对话、提示指令、候选性主张或任何凭据写入科研记忆。事实只有在来源可核验时才能标记为 `verified`。
- G7 必须同步形成中文全文、英文全文、全文大纲、论点—证据—实验—作用—意义链和双语对齐记录。两种语言共享相同数字、单位、公式、图表、引文和结论边界。

## 环境与权限

- 读取 `codex-policy.toml` 和未提交的 `.sciops-local.toml`。
- `guided` 模式可自动执行只读检查和已有环境中的测试；安装缺失依赖前说明影响。
- `trusted` 模式可自动执行项目目录内、可逆、来源固定的依赖安装与 CodeGraph 索引更新。
- 系统级安装、管理员权限、凭据申请或更换、登录、付费操作、外部发布、删除和不可逆操作仍需遵守当前 Codex 权限与确认要求。任何配置都不能绕过平台策略。
- 真实 API key 只允许存在于未跟踪的 `.env`、`.sciops-credentials.local.json` 或环境变量，不得写入源码、科研记忆、状态文件、日志、提交或发布包。GitHub CLI 的 OAuth 配置保持在其本机配置目录，不复制到科研凭据 JSON。

## CodeGraph

- `.codegraph/` 存在且 MCP 可用时，代码探索优先调用 `codegraph_explore`；健康状态由 `sciops codex resume` 返回。
- CodeGraph 负责代码索引，不替代 `research-state.yaml` 中的科研状态。
- 文件更新由 MCP 监听器自动同步；受限环境或批处理开始前可运行 `SYNC-CODEX`。

## 沟通

- 保留完整技术准确性，删除寒暄、重复计划和无信息量说明。
- 默认用用户当前语言，关键判断给出证据、风险和下一动作。
- 安全警告、不可逆确认和容易误解的多步操作使用完整句子，不做过度压缩。

## 完成门槛

- 代码修改后运行相关测试、`ruff` 和构建检查。
- 科研产出通过阶段审计、写作质检和引用核验后才能标记完成。
- 结束前更新活动项目状态，确保下一次 Codex 会话可直接续接。
